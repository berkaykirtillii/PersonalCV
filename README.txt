Since this about me page is extracted usign heroku, there are composer.json
and index.php file(I combined it with php because its dynamic when you use php.
Static pages cannot bu used in heroku.) I wanted to use heroku because I found 
it more useful.
